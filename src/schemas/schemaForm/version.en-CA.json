{
    "schema": "version",
    "title": "Version",
    "type": "string",
    "description": "The schema version used to validate the configuration file. The schema should enumerate the list of versions accepted by this version of the viewer.",
    "properties": {
        "version": {
            "title": "Version",
            "type": "string",
            "enum": [
                "3.2.0",
                "3.3.2"
            ],
            "default": "3.2.0",
            "description": "The viewer version used to validate the configuration file. The fields with the highest version have a turquoise left border."
        }
    }
}
