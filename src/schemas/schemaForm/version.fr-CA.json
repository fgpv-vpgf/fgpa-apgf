{
    "schema": "version",
    "title": "Version",
    "type": "object",
    "description": "La version du schema utilisé pour valider le fichier de configuration.  The schema should enumerate the list of versions accepted by this version of the viewer.",
    "properties": {
        "version": {
            "title": "Version",
            "type": "string",
            "enum": [
                "2.3",
                "2.4"
            ],
            "default": "2.3",
            "description": "La version du visualisateur utilisé pour valider le fichier de configuration."
        }
    }
}
